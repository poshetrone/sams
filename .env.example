# ============================================================
# SAMS — Variables d'environnement (front Next.js)
# Copier ce fichier en `.env.local` (ignoré par git) et renseigner les valeurs.
# ============================================================

# URL de base de l'API AdonisJS (api-sams). Publique : utilisée côté serveur
# (appels REST) ET côté navigateur (login Discord + temps réel SSE).
NEXT_PUBLIC_API_URL=http://localhost:3333
